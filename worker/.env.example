NODE_ENV=development

# Same as server .env
REDIS_URL=redis://localhost:6379
MONGODB_URI=mongodb://localhost:27017/notifyx

# Worker identity (for monitoring in metrics)
WORKER_ID=worker-1

# Concurrent jobs to process per worker instance
WORKER_CONCURRENCY=5

LOG_LEVEL=info
