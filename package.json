{
  "name": "notifyx",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup:hooks": "git config core.hooksPath .githooks",
    "scan:secrets:staged": "node scripts/secret-guard.cjs --mode=staged",
    "scan:secrets:range": "node scripts/secret-guard.cjs --mode=range",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "mongoose": "^9.6.1"
  }
}
